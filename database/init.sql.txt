-- Give縁 initial schema + seed data

CREATE TABLE IF NOT EXISTS users (
  id INT AUTO_INCREMENT PRIMARY KEY,
  email VARCHAR(255) NOT NULL UNIQUE,
  password_hash VARCHAR(255) NOT NULL,
  role ENUM('member', 'admin') NOT NULL DEFAULT 'member',
  name VARCHAR(100) DEFAULT NULL,
  phone VARCHAR(50) DEFAULT NULL,
  profile_image VARCHAR(255) DEFAULT NULL,
  first_login TINYINT(1) NOT NULL DEFAULT 1,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS expo_tokens (
  id INT AUTO_INCREMENT PRIMARY KEY,
  user_id INT NOT NULL,
  expo_token VARCHAR(255) NOT NULL UNIQUE,
  last_used_at TIMESTAMP NULL DEFAULT NULL,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

INSERT INTO users (email, password_hash, role, name, phone, profile_image, first_login)
VALUES
  ('member@example.com', '$2y$12$eEQ.O9oCitg43RNqlM7fCe1XIK/5oQ/hAvZVT9OMzXsrKH8Hjgaqe', 'member', 'テスト会員', '090-0000-0000', 'uploadimg/logotest.png', 1),
  ('admin@example.com', '$2y$12$Reyj4jQ680/QCmDUSqPshewKuX0YkHenw3Q32HORtQtu7N6q3v.YW', 'admin', '管理者アカウント', '090-1111-1111', 'uploadimg/logotest.png', 0);

INSERT INTO expo_tokens (user_id, expo_token, last_used_at)
VALUES
  (1, 'TESTEXPO123456', NOW());
